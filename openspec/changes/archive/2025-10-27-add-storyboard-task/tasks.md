## Tasks
- [x] Update workflow and CLI types to include `CREATE_STORYBOARD` with prerequisite checks and run-all sequencing.
- [x] Implement Gemini prompt builder and response validator for storyboard generation, including unit tests.
- [x] Persist storyboard results via stories repository and add workflow task tests (success, failures, duplicates).
- [x] Add stub fixtures, CLI coverage, and integration tests ensuring stub/real modes behave consistently.
- [x] Run `openspec validate add-storyboard-task --strict` and ensure all new specs pass.
