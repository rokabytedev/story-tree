# Story Tree UI Supabase configuration
#
# Copy this file to `.env.local` and provide your Supabase credentials.
# The UI uses the service role key to fetch server-side artifacts. Never expose
# these secrets in client-side code or commit them to version control.

# Select which Supabase project to target. Valid values: "local" (default) or "remote".
STORY_TREE_SUPABASE_MODE=local

# Local Supabase stack credentials (used when mode is "local" or omitted).
SUPABASE_LOCAL_URL=http://127.0.0.1:54321
SUPABASE_LOCAL_SERVICE_ROLE_KEY=your-local-service-role-key

# Remote Supabase credentials (used when STORY_TREE_SUPABASE_MODE=remote).
# SUPABASE_REMOTE_URL=https://your-project.supabase.co
# SUPABASE_REMOTE_SERVICE_ROLE_KEY=your-remote-service-role-key

# Optional final fallback recognized by shared Supabase tooling.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
